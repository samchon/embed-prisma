generator client {
  provider        = "prisma-client"
  output          = "output"
  previewFeatures = ["postgresqlExtensions", "views"]
}

datasource db {
  provider   = "postgresql"
  extensions = [pg_trgm]
}

generator markdown {
  provider = "prisma-markdown"
  title    = "Shopping Mall"
  output   = "../../docs/ERD.md"
}
